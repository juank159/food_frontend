# El build ahora se genera dentro del Dockerfile; "build" en el repo es
# un symlink a una carpeta local (para builds manuales en Mac) que no
# existe dentro del contenedor, así que hay que excluirlo sí o sí.
build
build/

.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies

# Git
.git/
